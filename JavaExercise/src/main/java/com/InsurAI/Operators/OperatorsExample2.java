package com.InsurAI.Operators;

public class OperatorsExample2 {
    public static void main(String[] args) {
        int age = 20;
        boolean eligible = (age >= 18) && (age <= 60);
        System.out.println("Is eligible for work: " + eligible);
    }
}
